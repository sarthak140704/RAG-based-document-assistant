[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # line length handled by the formatter/reviewer, not enforced

[tool.ruff.lint.isort]
known-first-party = ["src"]
